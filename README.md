 How to run it 

1. Clone the repo
   `git clone https://github.com`

2. Install dependencies (Do this first!)
   `npm install`

3. Start the project
   `npm run dev`

4. View the site
   Click the link in your terminal (usually `http://localhost:5173`)
